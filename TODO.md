# TODO

Open items for the site. Nothing here goes on the page until the owner confirms it.

## Before merging `redesign` into `main`

- [ ] Replace `assets/CV.pdf` with the current CV compiled from `cv.tex` (the file in the repo is the old one; the "Download CV" button points to it).
- [ ] Eyeball the page in a browser (light and dark mode, phone width) — no screenshot tooling on this machine, so this is a manual check.
- [ ] Decide whether `cv.tex` and `website-brief (2).md` get committed (e.g. under `cv/`) or gitignored.

## Content pending owner confirmation (from the brief's "Open items")

- [ ] LinkedIn URL — add to hero links and contact section.
- [ ] City to display — add to contact section.
- [ ] The MPI paper: which stay it belongs to, title, co-authors, status, preprint link. Until then the MPI MiS entry does not mention a paper at all.
- [ ] Official name of the UPC course taught (Teaching Assistant entry).
- [ ] Outlier AI: exact contracted job title, and whether an NDA restricts the description.
- [ ] Cangur Olympiad category/level.
- [ ] Literature contests: names, years, placements (currently a single generic line).
- [ ] Whether to include the 2023 Spanish National Chemistry Olympiad participation (currently omitted).
- [ ] Whether any content from the old site should come back (the Ecosystem Simulation project and its PDFs were dropped because the brief lists only two projects).
- [ ] Final-year project (TFG/TFM) — add once assigned.

## Nice to have

- [ ] Open Graph image (currently none, since there is no photo; could be a text/monogram card).
- [ ] Self-host the two Google Fonts to remove the external request.
